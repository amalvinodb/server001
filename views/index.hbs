<section>
    <div class="container mt-5">
        <div class="row">
            {{#each product}}
            <div class="card" style="width: 18rem;">
                <img src="{{this.image}}" class="card-img-top" alt="an-image">
                <div class="card-body">
                    <h5 class="card-title">{{this.name}}</h5>
                    <p class="card-text">{{this.discription}}</p>
                    <a href="#" class="btn btn-primary">ckick me</a>
                </div>
            </div>
            {{/each}}
        </div>

    </div>
</section>